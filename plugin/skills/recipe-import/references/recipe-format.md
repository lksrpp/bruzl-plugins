# Bruzl recipe formatting spec

> Kept in sync with the AI service `recipe-transform` prompt (`ai-service/src/cookbook_ai/llm/prompts.py` in the private `lksrpp/cookbook` repo). Shared domain rules cross over; output-framing changes do not.

🚧 WIP — to be lifted near-verbatim from the `recipe-transform` system prompt per build plan §1.3, with field names reframed to the `create_recipe` tool args (camelCase: `sourceUrl`, `cover`, `categories`).
